package p1;

public interface Moveable { //methods in interface are not implemented (Only given a name)
	//abstract class can have instances and variables
	// functional interface; only one abstract method
	void fly();
	void run();
	void walk(); 

}
